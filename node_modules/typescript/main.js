//string
//number
//boolean
//null
//undefined
//--------

//symbol
//bigint
// let a=true
// let a:string = 3
// let a:boolean = 3
// let b:null =null
// let c:undefined=undefined
// var a=3;
// a = 4

// a=9.0
// a=8.8
// a="string"
// a=false
// let a:number =3;
// a = 4;
// var b =null;
// var c=undefined;
// let b: null=null;
// let c: undefined=undefined;

// function double(n){
//     return n * 2;
// }
// console.log(double(4));
// console.log(double("hello"));
// let userName: string; // 사용자 이름
// let userAge: number; // 사용자 나이
// let isAdmin: boolean; // 예: 관리자 여부

// userName = "Alice";
// userAge = 25;
// isAdmin = true;
//변수들의 타입을 지정
//変数のタイプを指定

let userName:string = "Alice";
let userAge:string = 25;
let isAdmin:boolean = true;
// let c:undefined=undefined
// 변수 선언과 초기값 지정
let productName:string; // 상품 이름
let productPrice:string ; // 상품 가격
let isAvailable:boolean; // 상품 재고 여부
// 값 할당入れる
productName = "Smartphone";
productPrice = "$500";
isAvailable = true;
// 예시 출력出力
console.log(`상품명: ${productName}, 가격: ${productPrice}, 재고 여부: ${isAvailable}`);

//문제 3. 두 숫자를 더하는 함수를 작성하고, 함수의 매개변수와 반환값에console.log(addNumbers(5, 3));
// function addNumbers(num1: number, num2: number): number {
//     return num1 + num2;
// }

console.log(addNumbers(5, 3));
// 値を受け文字列で変換する함수作成 値が null また、 undefinedである場合"値なしを返す"を返す
// 一つの変数にタイップを色々と受けたいばあい（ユニオンタイプ）を使える
//(例: 文字列もしくは数字: string|number)
function stringifyValue(value) {
    if (value === null || value === undefined) {
        return "値なし";
    } else {
        return String(value);
    }
  }
  
  // 함수 호출
  console.log(stringifyValue("Hello")); // "Hello"
  console.log(stringifyValue(null)); // "값이 없습니다"
  console.log(stringifyValue(undefined)); // "값이 없습니다"


  function compareValues(a: unknown, b: unknown): string {
    if (a === b) {
      return "엄격한 동등성";
    } else if (a == b) {
      return "느슨한 동등성";
    } else {
      return "동등하지 않음";
    }
  }
  
  // 함수 호출 예시
  console.log(compareValues(5, "5")); // ?
  console.log(compareValues(null, undefined)); // ?
  console.log(compareValues(false, 0)); // ?
  console.log(compareValues(NaN, NaN)); // ?
  console.log(compareValues(42, 42)); // ?